# DESIGN.md

## 气质与意象
深夜工位上的终端窗口，黑色背景上只有代码和光标在跳动——安静、专注、无干扰。工具本身就是内容，不需要装饰。

## 视觉策略
- 深色主题为默认，减少视觉疲劳
- 信息密度适中，关键区域留足呼吸空间
- HTTP 方法用颜色编码：GET=绿、POST=琥珀、PUT=蓝、DELETE=红、PATCH=紫

## 配色方案
- 背景：近黑（`oklch(0.145 0 0)`），卡片/面板稍亮（`oklch(0.205 0 0)`）
- 主文字：高对比浅色（`oklch(0.985 0 0)`）
- 次要文字：中灰（`oklch(0.556 0 0)`）
- 强调色：终端绿 `#4ade80` 用于成功状态
- 边框：极淡分割（`oklch(0.25 0 0)`）

## 字体排版
- UI 字体：系统默认无衬线（已内置 PingFang SC / Microsoft YaHei）
- 代码/请求体/响应体：等宽字体 `ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas`
- 代码区字号 13px，行高 1.6

## 页面结构
- 顶部：工具名称 + 暗色模式切换
- 左侧（可选）：请求历史列表
- 主区域上半：请求配置（URL 栏 + 方法选择 + 发送按钮 → Headers/Body/Params 标签页）
- 主区域下半：响应面板（状态码 + 耗时 → Body/Headers/Cookies 标签页）
- 上下分栏，可拖拽调整比例

## 动效与交互
- 发送按钮点击后短暂 loading 旋转
- 响应到达后 body 区域淡入
- 标签页切换无动画，即切即显，保持效率感
- 状态码颜色即时变化

## 设计禁忌
- 不用渐变背景、不用毛玻璃效果
- 不用圆角超过 12px 的大圆角
- 不在工具界面放装饰性插图或 icon 插画
- 不用动画延迟用户操作
